---
title: get_instance yöntemi
second_title: Aspose.Cells for Python via .NET API Referanslar
description:
type: docs
weight: 20
url: /tr/aspose.cells/cellsdatatablefactory/get_instance/
is_root: false
---
##  get_instance(self, collection) {#list}
Verilen koleksiyondan ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, collection):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| collection | list | tabloyu oluşturmak için koleksiyon|


##  get_instance(self, vals, column_names) {#list-list}
Verilen int değer dizisinden ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, vals, column_names):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| vals | list | tabloyu oluşturmak için int değerleri|
| column_names | list | Tablonun sütun adları.<br/>Uzunluğu yalnızca 1 (dikey olarak int değerlerine göre tabloyu oluştur) olabilir<br/> veya int değerlerinin uzunluğu (tabloyu yatay olarak int değerlerine göre oluştur)|


##  get_instance(self, vals, vertial) {#list-bool}
Verilen int değer dizisinden ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, vals, vertial):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| vals | list | tabloyu oluşturmak için int değerleri|
| vertial | bool |tabloyu int değerlerine göre dikey (doğru) veya yatay (yanlış) olarak mı oluşturacağız?|


##  get_instance(self, vals, column_names) {#list-list}
Verilen çift değer dizisinden ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, vals, column_names):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| vals | list | tabloyu oluşturmak için çift değerler|
| column_names | list | Tablonun sütun adları.<br/>Uzunluğu sadece 1 olabilir (çift değerlerle dikey olarak tabloyu oluştur)<br/> veya çift değerlerin uzunluğu (çift değerlere göre yatay olarak tabloyu oluştur)|


##  get_instance(self, vals, vertial) {#list-bool}
Verilen çift değer dizisinden ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, vals, vertial):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| vals | list | tabloyu oluşturmak için çift değerler|
| vertial | bool | tabloyu çift değerlere göre dikey (doğru) veya yatay (yanlış) olarak mı oluşturacağız?|


##  get_instance(self, vals, column_names) {#list-list}
Verilen nesne dizisinden ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, vals, column_names):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| vals | list | tabloyu oluşturmak için nesneler|
| column_names | list | Tablonun sütun adları.<br/>Uzunluğu sadece 1 (nesneleri dikey olarak tabloya yerleştir) olabilir<br/> veya nesnelerin uzunluğu (nesneleri yatay olarak sıralayarak tablo oluşturun)|


##  get_instance(self, vals, vertial) {#list-bool}
Verilen nesne dizisinden ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, vals, vertial):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| vals | list | tabloyu oluşturmak için nesneler|
| vertial | bool | Tablonun nesnelere göre dikey (doğru) veya yatay (yanlış) olarak mı oluşturulacağı|


##  get_instance(self, collection, has_header) {#list-bool}
Verilen koleksiyondan ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, collection, has_header):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| collection | list | tabloyu oluşturmak için koleksiyon|
| has_header | bool | İlk satırın başlık olup olmadığını gösterir|


##  get_instance(self, vals, has_header, column_names) {#list-bool-list}
Verilen nesne dizisinden ICellsDataTable oluşturur.


###  İadeler

ICellsDataTable örneği


```python

def get_instance(self, vals, has_header, column_names):
    ...
```


| Parametre| Tip| Tanım|
| :- | :- | :- |
| vals | list | tabloyu oluşturmak için nesneler|
| has_header | bool | İlk satırın başlık satırı olup olmadığını gösterir.|
| column_names | list | Tablonun sütun adları.<br/>Uzunluğu sadece 1 (nesneleri dikey olarak tabloya yerleştir) olabilir<br/> veya nesnelerin uzunluğu (nesneleri yatay olarak sıralayarak tablo oluşturun)|



###  Ayrıca bakınız
* modül [`aspose.cells`](../../)
* sınıf [`CellsDataTableFactory`](/cells/python-net/tr/aspose.cells/cellsdatatablefactory)
